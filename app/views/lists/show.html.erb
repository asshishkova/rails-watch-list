<h1>This list info</h1>
<%= @list.name  %>
<br>

<ul>
  <% @list.movies.each do |movie| %>
    <li> <%= movie.title %> </li>
  <% end %>
</ul>

<%= link_to 'Add a movie', "/lists/#{@list.id}/bookmarks/new" %>
<br>
<%= link_to 'Back', lists_path %>
